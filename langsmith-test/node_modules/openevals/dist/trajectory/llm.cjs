"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createTrajectoryLLMAsJudge = exports.TRAJECTORY_ACCURACY_PROMPT = exports.TRAJECTORY_ACCURACY_PROMPT_WITH_REFERENCE = void 0;
const llm_js_1 = require("../llm.cjs");
const utils_js_1 = require("../utils.cjs");
const utils_js_2 = require("./utils.cjs");
const accuracy_js_1 = require("../prompts/trajectory/accuracy.cjs");
var accuracy_js_2 = require("../prompts/trajectory/accuracy.cjs");
Object.defineProperty(exports, "TRAJECTORY_ACCURACY_PROMPT_WITH_REFERENCE", { enumerable: true, get: function () { return accuracy_js_2.TRAJECTORY_ACCURACY_PROMPT_WITH_REFERENCE; } });
var accuracy_js_3 = require("../prompts/trajectory/accuracy.cjs");
Object.defineProperty(exports, "TRAJECTORY_ACCURACY_PROMPT", { enumerable: true, get: function () { return accuracy_js_3.TRAJECTORY_ACCURACY_PROMPT; } });
function _formatInputs(params) {
    const { outputs, referenceOutputs } = params;
    const normalizedOutputs = (0, utils_js_1._normalizeToOpenAIMessagesList)(outputs);
    const normalizedReferenceOutputs = (0, utils_js_1._normalizeToOpenAIMessagesList)(referenceOutputs ?? []);
    const formattedReferenceOutputs = normalizedReferenceOutputs?.length
        ? (0, utils_js_2._chatCompletionMessagesToString)(normalizedReferenceOutputs)
        : "";
    const formattedOutputs = (0, utils_js_2._chatCompletionMessagesToString)(normalizedOutputs);
    return [formattedOutputs, formattedReferenceOutputs];
}
/**
 * Creates an evaluator that uses an LLM to judge agent trajectories.
 *
 * @param options.prompt - The evaluation prompt. Defaults to TRAJECTORY_ACCURACY_PROMPT_WITH_REFERENCE.
 * @param options.feedbackKey - Key used to store the evaluation result. Defaults to "trajectory_accuracy".
 * @param options.model - Model identifier to use.
 * @param options.judge - The LLM used for evaluation.
 * @param options.continuous - If true, score will be a float between 0 and 1. Defaults to false.
 * @param options.choices - Optional list of specific float values the score must be chosen from.
 * @param options.useReasoning - If true, includes explanation for the score. Defaults to true.
 * @param options.fewShotExamples - Optional list of example evaluations.
 * @returns A function that evaluates agent trajectories using the configured LLM judge.
 */
const createTrajectoryLLMAsJudge = ({ prompt = accuracy_js_1.TRAJECTORY_ACCURACY_PROMPT_WITH_REFERENCE, feedbackKey = "trajectory_accuracy", ...rest } = {}) => {
    const scorer = (0, llm_js_1._createLLMAsJudgeScorer)({
        prompt,
        ...rest,
    });
    const wrappedEvaluator = async ({ inputs, outputs, referenceOutputs, ...extra }) => {
        const [formattedOutputs, formattedReferenceOutputs] = _formatInputs({
            outputs,
            referenceOutputs,
        });
        return (0, utils_js_1._runEvaluator)(`llm_as_${feedbackKey}_judge`, scorer, feedbackKey, {
            inputs,
            outputs: formattedOutputs,
            referenceOutputs: formattedReferenceOutputs,
            ...extra,
        });
    };
    return wrappedEvaluator;
};
exports.createTrajectoryLLMAsJudge = createTrajectoryLLMAsJudge;
